# XDB-Node Git Ignore Configuration
# This configuration ensures that temporary files and build artifacts are not tracked.
# Documentation: https://git-scm.com/docs/gitignore

# Dependencies
# External packages should be installed via package manager, not committed
node_modules/
jspm_packages/

# Build Artifacts
# Compiled output and build metadata for the driver
dist/
build/
*.tsbuildinfo

# Test and Coverage
# Results from unit tests and code coverage tools
coverage/
.nyc_output/

# Logs and Debugging
# Runtime logs and package manager error reports
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Environment Variables and Secrets
# Confidential configuration and local environment overrides
# .env
# .env.local
# .env.*.local
.npmrc

# Operating System Specific Files
# Metadata files generated by macOS and Windows
.DS_Store
Thumbs.db

# Persistence and Data
# Local storage and data files
# Keep configuration files but ignore actual runtime data
data/
*.json
!package.json
!tsconfig.json
!package-lock.json
